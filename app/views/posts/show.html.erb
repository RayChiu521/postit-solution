<% if logged_in? %>
	<p class="pull-right">
		[<%= link_to "edit", edit_post_path(@post) %>]
	</p>
<% end %>

<p>
	<%= render @post.categories %>
</p>
<h4><%= link_to @post.title, fix_url(@post.url) %></h4>
<p>
	<%= @post.description %>
</p>

<hr/>

<% if logged_in? %>
	<%= form_for [@post, @comment] do |f| %>
		<%= render "shared/errors", obj: @comment %>

		<div class="control-group">
			<%= f.label :body, "Leave a comment" %>
			<%= f.text_area :body, rows: 5, class: "span4" %>
		</div>

		<%= f.submit "Create comment", class: "btn btn-primary" %>
	<% end %>
<% end %>

<h5>Comments:</h5>

<% @post.comments.each do |comment| %>
<div class="row">
	<% if logged_in? %>
		<div class="span0 well">
			<%= link_to vote_post_comment_path(@post, comment, vote: true), title: "good", method: "post" do %>
				<i class="icon-arrow-up"></i>
			<% end %>
			<br />
			<%= Vote.total_votes(comment) %>
			<br />
			<%= link_to vote_post_comment_path(@post, comment, vote: false), title: "bad", method: "post" do %>
				<i class="icon-arrow-down"></i>
			<% end %>
		</div>
	<% end %>

  <div class="well span8">
  	<em>"<%= comment.body %>"</em>
  	<br/>
  	<span class="quiet">by</span> <%= comment.creator.username %>
  	<small>at <%= display_datetime(comment.created_at) %></small>
  </div>
</div>
<% end %>